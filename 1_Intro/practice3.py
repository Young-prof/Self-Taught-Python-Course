# Write a python Program to print the contents of a directory using os modules. search online for the function which does that
import os  # An os module which is an internal module.
# A function to display all the the files in the Current working directory
print(os.listdir())
